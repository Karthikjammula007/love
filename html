<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Will You Be My Valentine?</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f8f0f8;
            margin: 0;
            font-family: 'Aesthetic', sans-serif;
            overflow: hidden;
            position: relative;
        }

        .content {
            text-align: center;
            opacity: 0;
        }

        .heart {
            font-size: 150px;
            color: #f07a9c;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(2);
            opacity: 1;
        }

        h1 {
            font-size: 3rem;
            color: #f07a9c;
            opacity: 0;
            transition: opacity 1.5s ease-in;
        }

        .button {
            padding: 10px 20px;
            background-color: #f07a9c;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-size: 1.2rem;
            transition: background-color 0.3s;
            margin: 10px;
            display: inline-block;
            position: relative;
            cursor: pointer;
        }

        .button:hover {
            background-color: #d96682;
        }

        .happyMessage {
            font-size: 2rem;
            color: #4caf50;
            display: none;
        }
    </style>
</head>
<body>
    <div class="heart">💖</div>
    <div class="content">
        <h1 id="valentineText">Will You Be My Valentine?</h1>
        <div id="yesButton">
            <a class="button" id="yesBtn">Yes, I Will!</a>
        </div>
        <div id="noButton">
            <a class="button" id="noBtn">No</a>
        </div>
        <div class="happyMessage" id="happyMsg">Yay! I’m so happy! 💖</div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const heart = document.querySelector(".heart");
            const content = document.querySelector(".content");
            const valentineText = document.getElementById("valentineText");

            setTimeout(() => {
                heart.style.transition = "transform 2s ease-out, opacity 1.5s ease-out";
                heart.style.transform = "translate(-50%, -50%) scale(0.5)";
                heart.style.opacity = "0";
            }, 500);

            setTimeout(() => {
                content.style.transition = "opacity 1.5s ease-in";
                content.style.opacity = "1";
                valentineText.style.opacity = "1"; // Ensure the text appears
            }, 2000);
        });

        document.getElementById("yesBtn").addEventListener("click", () => {
            const happyMsg = document.getElementById("happyMsg");
            happyMsg.style.display = "block";
            happyMsg.style.opacity = "0";
            happyMsg.style.transition = "opacity 1s ease-in-out";
            setTimeout(() => {
                happyMsg.style.opacity = "1";
                setTimeout(() => {
                    window.location.href = "https://www.instagram.com";
                }, 2000);
            }, 100);
        });

        function moveButton() {
            const noBtn = document.getElementById("noBtn");
            const maxX = window.innerWidth - noBtn.clientWidth;
            const maxY = window.innerHeight - noBtn.clientHeight;
            const randomX = Math.random() * maxX;
            const randomY = Math.random() * maxY;

            noBtn.style.position = "absolute";
            noBtn.style.transition = "left 0.3s ease-in-out, top 0.3s ease-in-out";
            noBtn.style.left = `${randomX}px`;
            noBtn.style.top = `${randomY}px`;
        }

        document.getElementById("noBtn").addEventListener("mouseover", moveButton);
        document.getElementById("noBtn").addEventListener("touchstart", moveButton); // For mobile users
    </script>
</body>
</html>
